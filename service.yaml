apiVersion: v1
kind: Service
metadata:
  name: trend-service
  labels:
    tier: frontend
spec:
  type: LoadBalancer
  selector:
    app: trend
  ports:
    - port: 3000
      targetPort: 80
      protocol: TCP
      
      
